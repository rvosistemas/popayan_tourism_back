meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://127.0.0.1:8001/user_profile/login/
  body: json
  auth: none
}

body:json {
  {
    "username": "{{username}}",
    "password": "{{password}}"
  }
}

tests {
  test("get token", function() {
  const data = res.body;
  let token = bru.setEnvVar("auth_token", data.token);
  });
}
